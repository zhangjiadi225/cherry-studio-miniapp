# @cherry-miniapp/kit

Cherry local miniapp 的宿主适配包。它提供 `window.cherry` 类型、流式 AI 调用与取消、模型槽位可用性、JSON 存档、文件导出、前台剪贴板、运行时能力快照和可显式安装的浏览器开发 mock。

产品仓库只从公开入口导入；不要深层导入 `src/`。正式环境使用 Cherry 用户自己的模型槽位，不要求产品持有模型 API Key。
